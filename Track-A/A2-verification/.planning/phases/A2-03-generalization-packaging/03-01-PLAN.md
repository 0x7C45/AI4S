---
phase: A2-03-generalization-packaging
plan: 01
type: execute
wave: 1
depends_on: ["A2-02"]
files_modified:
  - Track-A/A2-verification/src/rtl_parser.py
  - Track-A/A2-verification/src/coverage_gen.py
  - Track-A/A2-verification/src/skeleton_gen.py
  - Track-A/A2-verification/src/dead_code_analyzer.py
  - Track-A/A2-verification/src/sim_runner.py
  - Track-A/A2-verification/run.py
  - Track-A/A2-verification/run.sh
  - Track-A/A2-verification/wheelhouse/
  - Track-A/A2-verification/README.md
  - Track-A/A2-verification/THIRD_PARTY.md
autonomous: true
requirements:
  - A2-08
  - A2-09

must_haves:
  truths:
    - "rtl_parser 对 case2-5 各自解析出 design.json 且 inferred_protocols 字段非空"
    - "coverage_gen 对非 AXI 接口（SRAM/valid-ready）按协议选对应 bin 模板"
    - "skeleton_gen 对非 AXI 接口生成可编译 testbench（不因缺 AXI 端口 crash）"
    - "sim_runner Docker timeout=1200s，case1 5000 笔能跑完"
    - "wheelhouse/ 含 cocotb/cocotbext-axi/jinja2/pyverilog 的 manylinux2014_x86_64 wheel（≥4 个）"
    - "run.sh 含离线安装步骤（pip install --no-index --find-links=wheelhouse）"
    - "THIRD_PARTY.md 含全部 8 个依赖的版本/许可/调用边界"
    - "case1-5 各自跑通 run.py 产出 7 JSON，至少 3 个 case 的 coverage_result.json composite.percent > 0"
    - "框架代码无 case1-5 特定硬编码（grep case[1-5] src/ templates/ 无业务逻辑命中）"
  artifacts:
    - "Track-A/A2-verification/wheelhouse/（离线 wheel 目录）"
    - "Track-A/A2-verification/README.md（更新）"
    - "Track-A/A2-verification/run.sh（更新含离线安装）"
    - "Track-A/A2-verification/THIRD_PARTY.md（更新完整）"
  key_links:
    - "rtl_parser 协议推断对 SRAM/valid-ready 接口输出对应 inferred_protocols"
    - "coverage_gen 按 inferred_protocols 分派到 4 类 bin 模板"
    - "skeleton_gen 对非 AXI 接口降级生成最小 testbench"
    - "run.py 仿真阶段 sim_runner timeout=1200s"
    - "run.sh 调用 pip install --no-index --find-links=wheelhouse 离线装依赖"
---

# Plan 03-01: 通用化 + 离线打包

## Objective

将 Phase 1/2 的 case1 验证管道通用化到 case2-5（含非 AXI 接口），精化死码过滤，优化 5000 笔性能，补全离线打包（wheelhouse + README + THIRD_PARTY），5 case Docker 复验。产出评测可提交的完整包。

## Context

- Phase 1/2 仅在 case1（AXI 读适配器）验证通过
- case2-5 结构各异：case2 axis_fifo_adapter（streaming）、case3 axi_vfifo_raw（三时钟域）、case4 axi_ram、case5 axi_crossbar_addr
- Phase 2 遗留：死码过滤 if/else 边界不准（保守关闭）、5000 笔超时

### Locked Decisions
- **D-19**: 死码过滤精化（或显式保守关闭并在 coverage_collect 注明）
- **D-20**: sim_runner timeout=1200s
- **D-21**: wheelhouse manylinux2014_x86_64
- **D-22**: run.sh 离线安装
- **D-23**: 红线 #2/#5 合规

## Tasks

<task id="1" wave="1">
<name>通用化改造 — rtl_parser/coverage_gen/skeleton_gen 适配非 AXI 接口</name>
<action>
更新 src/rtl_parser.py、src/coverage_gen.py、src/skeleton_gen.py 适配 case2-5 的非 AXI 接口（per A2-08, D-23 红线 #5）：

rtl_parser.py：
- 确认 SRAM-like 协议推断（addr/din/dout/we/en 端口模式）对 case4（axi_ram）生效
- 确认 valid-ready stream 推断（valid/ready 端口对）对 case2（axis_fifo_adapter）生效
- 非 AXI 接口的 design.json inferred_protocols 字段非空

coverage_gen.py：
- 确认 _detect_active_templates 对 SRAM/valid-ready 接口激活 MEMORY/DATAPATH 模板
- 非 AXI 接口不强制 AXI bin 模板（避免空 bin）

skeleton_gen.py + cocotb_tb.py.j2：
- 对非 AXI 接口（无 s_axi_/m_axi_ 前缀）生成最小 testbench：时钟/复位/通用驱动
- testbench 模板的 AXI 块用 {% if "AXI" in protocols %} 保护，非 AXI 时不渲染 AXI 驱动
- 非 AXI 接口降级：生成基本时钟+复位+简单读驱动（不 crash）
</action>
<read_first>
- E:/AI4S/Track-A/A2-verification/src/rtl_parser.py（_classify_protocol 函数）
- E:/AI4S/Track-A/A2-verification/src/coverage_gen.py（_detect_active_templates）
- E:/AI4S/Track-A/A2-verification/src/skeleton_gen.py
- E:/AI4S/Track-A/A2-verification/templates/cocotb_tb.py.j2
- E:/AI4S/Track-A/A2-verification/testcases/A2_public_dataset/case2/rtl/（axis_fifo_adapter，streaming 接口参考）
</read_first>
<acceptance_criteria>
- rtl_parser 对 case2（testcases/A2_public_dataset/case2/rtl/）解析返回 inferred_protocols 非空
- rtl_parser 对 case4 解析返回 inferred_protocols 非空（AXI 或 SRAM）
- coverage_gen 对非 AXI 接口的 case 不强制输出 AXI bin（active_templates 不含 AXI 时 bins 无 read_length 等）
- skeleton_gen 对 case2 调用 generate 不 crash（即使无 s_axi_ 端口）
- 生成的 testbench 对非 AXI 接口不渲染 AxiMasterRead（{% if %} 保护生效）
</acceptance_criteria>
<verify>
cd E:/AI4S/Track-A/A2-verification && python3 -c "
import sys; sys.path.insert(0,'.')
from src.rtl_parser import parse
for c in ['case2','case4','case5']:
    d=parse(f'testcases/A2_public_dataset/{c}/rtl', c)
    assert d.inferred_protocols, f'{c} no protocols'
    print(f'{c}: top={d.top_module} protocols={d.inferred_protocols}')
print('rtl_parser generalization OK')
"
cd E:/AI4S/Track-A/A2-verification && python3 -c "
import sys; sys.path.insert(0,'.')
from src.rtl_parser import parse
from src.skeleton_gen import generate
import tempfile
d=parse('testcases/A2_public_dataset/case2/rtl','case2')
r=generate(d, tempfile.mkdtemp(), 20260630, 100)
tb=open(r.testbench_path).read()
assert 'AxiMasterRead' not in tb or 'AXI' in str(d.inferred_protocols), 'case2 should not have AXI driver if not AXI'
print('skeleton_gen non-AXI OK')
"
</verify>
</task>

<task id="2" wave="1" depends_on="[]">
<name>死码过滤精化 + 5000 笔性能优化</name>
<action>
更新 src/dead_code_analyzer.py（per D-19）和 src/sim_runner.py（per D-20）：

dead_code_analyzer.py 精化 if/else 边界：
- 追踪 `if (PARAM)` 后的 begin/end 块或到下一个 `else`/`endgenerate` 的行区间
- 只标记 if 到 else 之间为死码，else 块保留可达
- 若精化后不可达行数仍 >200，降级为保守方案：coverage_collect 默认不传 dead_code_info（显式注释"死码过滤保守关闭，分母含全部行"）

sim_runner.py 性能：
- Docker subprocess timeout 从 600 提到 1200
- 容器内 make 加 COCOTB_LOG_LEVEL=WARNING（或 TESTCASE 日志降噪）
</action>
<read_first>
- E:/AI4S/Track-A/A2-verification/src/dead_code_analyzer.py
- E:/AI4S/Track-A/A2-verification/src/sim_runner.py
- E:/AI4S/Track-A/A2-verification/src/coverage_collect.py
</read_first>
<acceptance_criteria>
- dead_code_analyzer 对 case1（S=32/M=16）不可达行数 < 200（精化成功），或 coverage_collect 默认不传 dead_code_info（保守关闭且注释说明）
- sim_runner.py 的 subprocess.run timeout == 1200
- case1 5000 笔在 1200s 内完成（仿真 exit 0，可用 Docker 验证）
</acceptance_criteria>
<verify>
cd E:/AI4S/Track-A/A2-verification && python3 -c "
from src.dead_code_analyzer import analyze
di=analyze(['testcases/A2_public_dataset/case1/rtl/case1.v'],{'S_DATA_WIDTH':32,'M_DATA_WIDTH':16})
n=len(di.unreachable_lines)
print(f'unreachable: {n}')
if n >= 200:
    # 保守方案：检查 coverage_collect 默认不传 dead_code_info
    import inspect, src.coverage_collect as cc
    sig=str(inspect.signature(cc.collect))
    assert 'dead_code_info=None' in sig, 'conservative mode: dead_code_info must default None'
    print('conservative mode: dead_code filter OFF (documented)')
else:
    print('refined mode: dead_code filter ready')
print('dead_code strategy OK')
"
grep -q '1200' E:/AI4S/Track-A/A2-verification/src/sim_runner.py && echo "timeout 1200 OK"
</verify>
</task>

<task id="3" wave="2" depends_on="[1, 2]">
<name>wheelhouse 离线打包 + README + run.sh 离线安装 + THIRD_PARTY</name>
<action>
创建离线 wheel + 文档（per D-21, D-22）：

wheelhouse 生成（Docker linux/amd64 内）：
- docker run 容器内执行 pip download 生成 manylinux2014_x86_64 wheel 到 wheelhouse/
- 至少含 cocotb/cocotbext-axi/jinja2/pyverilog 的 .whl

run.sh 更新：
- 加入离线安装段：检测 wheelhouse/ 存在 → pip install --no-index --find-links=wheelhouse -r requirements.txt
- Docker 容器仍需 apt-get install python3-pip bootstrap

README.md：
- 新增「离线安装」章节（wheelhouse 路径 + pip --no-index 命令）
- 新增「运行」章节（run.sh 五参数 + Docker 示例）

THIRD_PARTY.md：
- 确认 8 依赖齐全（Python/cocotb/cocotb-test/cocotbext-axi/Jinja2/PyVerilog/z3-solver/Verilator）
</action>
<read_first>
- E:/AI4S/Track-A/A2-verification/requirements.txt
- E:/AI4S/Track-A/A2-verification/THIRD_PARTY.md
- E:/AI4S/Track-A/A2-verification/CONSTRAINTS.md（§12）
</read_first>
<acceptance_criteria>
- wheelhouse/ 目录存在且 .whl 文件数 ≥ 4
- wheelhouse/ 含 cocotb/cocotbext_axi/jinja2/pyverilog 的 wheel（文件名匹配）
- run.sh 含 "pip install --no-index --find-links=wheelhouse" 字符串
- README.md 含 "离线安装" 或 "offline" 章节
- THIRD_PARTY.md 含 8 个依赖条目
</acceptance_criteria>
<verify>
cd E:/AI4S/Track-A/A2-verification
COUNT=$(ls wheelhouse/*.whl 2>/dev/null | wc -l)
[ "$COUNT" -ge 4 ] && echo "wheelhouse: $COUNT whl OK" || echo "wheelhouse: only $COUNT (need >=4)"
ls wheelhouse/ | grep -i cocotb && ls wheelhouse/ | grep -i jinja2 && ls wheelhouse/ | grep -i pyverilog
grep -q 'no-index' run.sh && echo "run.sh offline OK"
grep -qi 'install\|offline\|离线' README.md && echo "README OK"
</verify>
</task>

<task id="4" wave="2" depends_on="[1, 2, 3]">
<name>5 公开 case Docker 复验 + 红线合规 + 最终验收</name>
<action>
对 case1-5 各自跑通完整流水线（per D-23），验证 run.sh 独自跑通：

Docker 复验（每个 case 用 100 笔加速）：
- 循环跑 case1-5：python3 run.py --rtl testcases/.../$case/rtl --top $case --out smoke_out/$case --seed 20260630 --num-seq 100
- 检查每个 case 产出 design.json + report.json + coverage_result.json

红线合规（CONSTRAINTS §11 #2/#5）：
- grep -rn 'case[1-5]' src/ templates/ 确认无业务逻辑硬编码（注释/文档除外）

最终验收：
- run.sh 可执行（bash -n 语法检查）
- run.sh 含 run.py 调用
- case1-5 至少 3 个 composite.percent > 0
</action>
<read_first>
- E:/AI4S/Track-A/A2-verification/run.py
- E:/AI4S/Track-A/A2-verification/run.sh
- E:/AI4S/Track-A/A2-verification/CONSTRAINTS.md（§11）
</read_first>
<acceptance_criteria>
- case1-5 各自产出 design.json + report.json（至少 4/5 成功）
- 至少 3 个 case 的 coverage_result.json composite.percent > 0
- run.sh 通过 bash -n 语法检查且含 run.py 调用
- grep 'case[1-5]' src/ templates/ 无业务逻辑硬编码（注释/testcases 路径除外）
</acceptance_criteria>
<verify>
cd E:/AI4S/Track-A/A2-verification
# 语法检查
bash -n run.sh && echo "run.sh syntax OK"
grep -q 'run.py' run.sh && echo "run.sh calls run.py OK"
# 红线扫描
HITS=$(grep -rn 'case[1-5]' src/ templates/ 2>/dev/null | grep -v '#' | grep -v 'testcase' | grep -v 'public_dataset' | wc -l)
[ "$HITS" -eq 0 ] && echo "red-line #2 OK (no hardcode)" || echo "WARNING: $HITS potential hardcode hits"
# case 产物检查（需先跑 run.py）
for c in case1 case2 case3 case4 case5; do
  [ -f "smoke_out/$c/coverage_result.json" ] && echo "$c: coverage_result exists" || echo "$c: no coverage_result"
done
</verify>
</task>

## Risks

| Risk | Mitigation |
|------|------------|
| case2-5 非 AXI 接口 testbench 编译失败 | Task 1 通用化改造兜底 + 非 AXI 降级最小 testbench |
| 5000 笔仍超时 | 评测用 5000，调试用 100 笔验证管道 |
| wheelhouse 主机平台不匹配 | Docker linux/amd64 内生成 |
| 个别 case 仿真失败 | report.json 记录失败，不阻断其他 case |

## Artifacts this phase produces

- `Track-A/A2-verification/wheelhouse/` — 离线 wheel 目录
- `Track-A/A2-verification/README.md` — 离线安装运行说明（更新）
- `Track-A/A2-verification/run.sh` — 离线安装 + 流水线（更新）
- `Track-A/A2-verification/THIRD_PARTY.md` — 确认完整（更新）
- `src/rtl_parser.py` — 通用化（更新）
- `src/coverage_gen.py` — 通用化（更新）
- `src/skeleton_gen.py` — 非 AXI 降级（更新）
- `src/dead_code_analyzer.py` — 精化（更新）
- `src/sim_runner.py` — timeout=1200（更新）
