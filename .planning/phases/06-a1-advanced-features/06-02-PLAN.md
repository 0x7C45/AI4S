---
phase: 06-a1-advanced-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Track-A/A1-simulator/src/sim_engine.h
  - Track-A/A1-simulator/src/sim_engine.cpp
  - Track-A/A1-simulator/src/eval_expr.cpp
  - Track-A/A1-simulator/Makefile
autonomous: true
requirements:
  - A1-PARSE-07
  - A1-SIM-02
  - A1-SIM-04
  - A1-CASE-01
  - A1-CASE-02
  - A1-CASE-03
must_haves:
  truths:
    - "basic03, basic04, and basic05 each pass a clean build, compile_sim, argument-free run, and exact output diff."
    - "The Phase 5 basic01, basic02, alu, and priority_encoder cases still produce exact zero-diff output after the runtime changes."
    - "Clocked processes observe real posedges, edge waits and delay events advance simulation time, and NBA updates commit before combinational propagation."
    - "The three target fixtures can use their include/macro definitions, parameterized child instances, unpacked memories, variable indexing, readmemh/readmemb, current $time, existing file I/O, $finish, and non-crashing $dumpvars paths."
  artifacts:
    - path: "Track-A/A1-simulator/src/sim_engine.cpp"
      provides: "Targeted discrete-time scheduler, process continuations, NBA commit, hierarchy/memory execution, and system-task dispatch"
    - path: "Track-A/A1-simulator/src/eval_expr.cpp"
      provides: "Memory element and indexed signal evaluation used by basic04/basic05"
    - path: "Track-A/A1-simulator/Makefile"
      provides: "Deterministic clean-build, compile_sim, and no-argument run entry points from testcase directories"
  key_links:
    - from: "always #delay clock process"
      to: "posedge wait continuation"
      via: "timestamped event queue observes the actual signal transition"
    - from: "NONBLOCKING_ASSIGN"
      to: "combinational propagation"
      via: "same-time RHS snapshots commit as one NBA batch before propagateSignals()"
    - from: "$readmemh/$readmemb"
      to: "memory[index] expression reads"
      via: "runtime-relative loader fills declared memory metadata consumed by eval_expr.cpp"
    - from: "parameterized child instance"
      to: "parent output net"
      via: "fixture-scoped prefix, parameter override, and named/positional port binding"
---

<objective>
Complete the narrowed A1 critical path for basic03/basic04/basic05 and preserve the Phase 5 exact-diff baseline.

Purpose: Only the three sequential/preprocessing/memory fixtures and the four existing Phase 5 regressions are immediate acceptance gates. Implement the smallest runtime changes that satisfy the locked decisions, using the frontend contracts already delivered by 06-01 and avoiding broad simulator generalization.

Output: A working targeted scheduler/runtime plus a reproducible clean-build exact-diff matrix for the three targets and four Phase 5 regression cases.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-a1-basic-simulator/05-01-SUMMARY.md
@.planning/phases/06-a1-advanced-features/06-CONTEXT.md
@.planning/phases/06-a1-advanced-features/06-DISCUSSION-LOG.md
@.planning/phases/06-a1-advanced-features/06-01-SUMMARY.md
@Track-A/A1-simulator/Makefile
@Track-A/A1-simulator/src/sim_engine.h
@Track-A/A1-simulator/src/sim_engine.cpp
@Track-A/A1-simulator/src/eval_expr.cpp
@Track-A/A1-simulator/src/system_funcs.cpp
@Track-A/A1-simulator/src/ast.h
@Track-A/A1-simulator/src/sim_data.h
@Track-A/A1-simulator/src/preprocessor.cpp
@Track-A/A1-simulator/testcases/sim_public/benchmark/basic03/rtl/dut.v
@Track-A/A1-simulator/testcases/sim_public/benchmark/basic03/tb/tb.v
@Track-A/A1-simulator/testcases/sim_public/benchmark/basic03/tb/output_ref.mem
@Track-A/A1-simulator/testcases/sim_public/benchmark/basic04/rtl/dut.v
@Track-A/A1-simulator/testcases/sim_public/benchmark/basic04/rtl/def.v
@Track-A/A1-simulator/testcases/sim_public/benchmark/basic04/tb/tb.v
@Track-A/A1-simulator/testcases/sim_public/benchmark/basic04/tb/output_ref.mem
@Track-A/A1-simulator/testcases/sim_public/benchmark/basic05/rtl/dut.v
@Track-A/A1-simulator/testcases/sim_public/benchmark/basic05/rtl/def.v
@Track-A/A1-simulator/testcases/sim_public/benchmark/basic05/tb/tb.v
@Track-A/A1-simulator/testcases/sim_public/benchmark/basic05/tb/output_ref.mem
</context>

<source_audit>
| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Narrowed Phase 6 gate: basic03/basic04/basic05 plus Phase 5 exact regression | 06-02 | COVERED | Tasks 1-3 implement and verify the immediate acceptance gate. |
| REQ | A1-PARSE-07 | Object macros and relative include preprocessing used by basic04/basic05 | 06-02 Task 2 | COVERED | Consumes the completed 06-01 frontend and verifies runtime behavior through the fixtures. |
| REQ | A1-SIM-02 | Basic NBA commit and post-NBA delta propagation | 06-02 Task 1 | COVERED | Targeted Active/NBA behavior only. |
| REQ | A1-SIM-04 | $time, readmemh/readmemb entry, existing file I/O, $finish, and non-crashing $dumpvars | 06-02 Task 2 | COVERED | Full VCD is explicitly excluded. |
| REQ | A1-CASE-01 | basic01-basic05, with basic03-basic05 as new target cases | 06-02 Tasks 1-3 | COVERED | Exact diff is required for all five basic cases. |
| REQ | A1-CASE-02 | alu regression | 06-02 Task 3 | COVERED | Exact zero-diff Phase 5 regression. |
| REQ | A1-CASE-03 | priority_encoder regression | 06-02 Task 3 | COVERED | Exact zero-diff Phase 5 regression. |
| REQ | A1-SIM-03 | Broad asynchronous-reset DFF semantics | NONE | DEFERRED | D-06 makes synchronous reset the target-case requirement; broad async semantics are deferred. |
| REQ | A1-CASE-04..07 | i2c, ip, axis_fifo, sha256 | NONE | DEFERRED | D-01 explicitly removes these from the immediate gate. |
| REQ | A1-INC-01, A1-PAR-01, A1-PAR-02, A1-CASE-08 | Incremental compilation, multicore synchronization, and GEMM | NONE | DEFERRED | D-11 and the locked deferred scope leave these to Phase 7. |
| RESEARCH | - | No RESEARCH.md supplied | 06-02 | COVERED | No external library or package install is introduced; planning uses the locked context and existing code. |
| CONTEXT | D-01..D-03 | Target order, exact per-case gate, and Phase 5 regression | 06-02 Task 3 | COVERED | The final matrix is basic03, basic04, basic05, then basic01, basic02, alu, priority_encoder. |
| CONTEXT | D-04..D-06 | Posedge, edge waits, #delay, NBA, post-NBA propagation, and synchronous reset | 06-02 Task 1 | COVERED | The scheduler is deliberately bounded to these target semantics. |
| CONTEXT | D-07..D-09 | Include/macros, memory loaders, time, file I/O, finish, and dumpvars | 06-02 Task 2 | COVERED | Existing frontend and file-I/O paths are reused and extended only where target fixtures require it. |
| CONTEXT | D-10..D-11 | Existing AST/value/prefix patterns and only the target child-instance paths | 06-02 Task 2 | COVERED | No general hierarchy elaborator or broad multi-file support is planned. |
</source_audit>

<tasks>
<task type="auto">
  <name>Task 1: Replace inline clock toggling with the targeted Active/NBA scheduler</name>
  <files>Track-A/A1-simulator/src/sim_engine.h, Track-A/A1-simulator/src/sim_engine.cpp</files>
  <dependencies>Requires the completed AST event-control/nonblocking nodes from 06-01; no later task can begin until the scheduler API and runtime path exist.</dependencies>
  <read_first>
    .planning/phases/06-a1-advanced-features/06-CONTEXT.md (D-02, D-03, D-04, D-05, D-06, D-10)
    .planning/phases/05-a1-basic-simulator/05-01-SUMMARY.md (Phase 5 propagation and zero-diff baseline)
    .planning/phases/06-a1-advanced-features/06-01-SUMMARY.md (existing frontend event and NONBLOCKING_ASSIGN contracts)
    Track-A/A1-simulator/src/sim_engine.h (compile/run boundary)
    Track-A/A1-simulator/src/sim_engine.cpp (current run(), propagateSignals(), execItem(), clock-toggle fallback, and NONBLOCKING_ASSIGN placeholder)
    Track-A/A1-simulator/src/ast.h (DELAY, EVENT_CTRL, ALWAYS_BLOCK, INITIAL_BLOCK, and NONBLOCKING_ASSIGN nodes)
    Track-A/A1-simulator/testcases/sim_public/benchmark/basic03/rtl/dut.v (posedge register and synchronous reset)
    Track-A/A1-simulator/testcases/sim_public/benchmark/basic03/tb/tb.v (always #5 clock, posedge wait, #1 observation, and $finish)
  </read_first>
  <action>
    Per D-04 and D-05, add a small deterministic event representation in sim_engine.h and use it in sim_engine.cpp to drive the existing AST interpreter. Track current simulation time, insertion order, active callbacks, NBA snapshots, and edge-wait continuations in a priority queue. Schedule recurring always-#delay clock assignments at their requested timestamps, schedule initial-block delay and edge-wait resumptions as continuations, and wake a posedge waiter only after comparing the old and committed new clock values. Remove the current inline clock-toggling loops and the invalid self-call used for NONBLOCKING_ASSIGN. At each timestamp, run Active statements, snapshot nonblocking RHS values, commit the complete NBA batch, detect transitions, and invoke bounded combinational propagation after the commit. Implement the synchronous reset branch required by basic03/basic05 per D-06, preserve blocking assignment behavior for Phase 5, stop on queue exhaustion or $finish, and keep the existing uint64_t signal maps, hierarchical prefixes, and propagateSignals() fixed-point behavior from D-10.
  </action>
  <acceptance_criteria>
    The runtime has an actual timestamped queue and explicit Active/NBA ordering; `always #5` produces real clock transitions; `@(posedge clk)` resumes only on a detected transition; `<=` RHS values are captured before a same-time batch commit; post-NBA combinational logic converges; synchronous reset and queue/$finish termination are deterministic; no Phase 5 combinational path is removed.
  </acceptance_criteria>
  <verification>
    Build the regenerated frontend and execute basic03 from its testcase directory through clean, compile_sim, argument-free run, and exact diff. Then execute the four Phase 5 cases with the same exact-diff command to catch scheduler regressions before adding memory/hierarchy behavior.
  </verification>
  <verify>
    <automated>set -euo pipefail; ROOT=/home/kali/Documents/github/AI4S; SIM="$ROOT/Track-A/A1-simulator"; for case in basic03 basic01 basic02 alu priority_encoder; do CASE_DIR="$SIM/testcases/sim_public/benchmark/$case"; FILELIST="$CASE_DIR/filelist.txt"; (cd "$CASE_DIR" &amp;&amp; make -C "$SIM" clean &amp;&amp; make -C "$SIM" build &amp;&amp; make -C "$SIM" compile_sim FILELIST="$FILELIST" TOP=tb &amp;&amp; make -C "$SIM" run FILELIST="$FILELIST" TOP=tb &amp;&amp; diff -u tb/output_ref.mem tb/output.mem); done</automated>
  </verify>
  <must_haves>
    - "basic03 output_ref.mem and output.mem are byte-for-byte identical after a clean build."
    - "The Phase 5 basic01/basic02/alu/priority_encoder exact-diff loop remains green."
    - "The old inline wait/clock approximation is no longer the execution path for target edge waits."
  </must_haves>
  <threat_model>
    <trust_boundaries>
      <boundary>RTL delay/event syntax to the event queue</boundary>
      <boundary>Active process state to NBA snapshots and committed signal values</boundary>
    </trust_boundaries>
    <stride>
      <threat id="T-06-01" category="Denial of Service" severity="high" disposition="mitigate">Bound delta iterations and reject non-advancing recurring events so malformed target input cannot hang the simulator.</threat>
      <threat id="T-06-02" category="Tampering" severity="high" disposition="mitigate">Commit only queued NBA snapshots at the timestamp and compare old/new values before waking edge waiters, preventing event-order corruption.</threat>
    </stride>
  </threat_model>
  <artifacts>
    - "Targeted scheduler declarations in Track-A/A1-simulator/src/sim_engine.h"
    - "Timestamped Active/NBA/delta execution in Track-A/A1-simulator/src/sim_engine.cpp"
    - "Clean basic03 and Phase 5 exact-diff evidence from the automated gate"
  </artifacts>
  <done>D-04 and D-05 are implemented for the target cases, basic03 passes exact diff from clean state, and all four Phase 5 regression cases remain exact zero-diff.</done>
</task>

<task type="auto">
  <name>Task 2: Add only the target fixture memory, preprocessing, hierarchy, and system-task semantics</name>
  <files>Track-A/A1-simulator/src/sim_engine.cpp, Track-A/A1-simulator/src/eval_expr.cpp, Track-A/A1-simulator/Makefile</files>
  <dependencies>Task 1 scheduler must pass basic03 and the Phase 5 regression; 06-01 already provides preprocessor/AST contracts.</dependencies>
  <read_first>
    .planning/phases/06-a1-advanced-features/06-CONTEXT.md (D-01, D-07, D-08, D-09, D-10, D-11)
    .planning/phases/06-a1-advanced-features/06-01-SUMMARY.md (completed include/macro and memory metadata contracts)
    Track-A/A1-simulator/src/preprocessor.cpp (relative include and object-macro behavior already available)
    Track-A/A1-simulator/src/ast.h (MEMORY_DECL, MODULE_INST, PARAM_OVERRIDE, PORT_CONN, and SYS_TASK nodes)
    Track-A/A1-simulator/src/sim_data.h (SignalDef, MemoryDef, parameter, and dependency metadata)
    Track-A/A1-simulator/src/sim_engine.cpp (buildModule(), flattening, lvalue writes, system-task dispatch, and current file path handling)
    Track-A/A1-simulator/src/eval_expr.cpp (BITSEL and current signal evaluation)
    Track-A/A1-simulator/Makefile (absolute simulator path and compile_sim/run recipes)
    Track-A/A1-simulator/testcases/sim_public/benchmark/basic04/rtl/def.v (BASIC_WIDTH/BASIC_DEPTH macros)
    Track-A/A1-simulator/testcases/sim_public/benchmark/basic04/rtl/dut.v (readmemh, memory[addr], add_leaf parameters and named ports)
    Track-A/A1-simulator/testcases/sim_public/benchmark/basic04/tb/tb.v (runtime-relative existing file I/O and #delay)
    Track-A/A1-simulator/testcases/sim_public/benchmark/basic05/rtl/def.v (BASIC05_WIDTH/BASIC05_DEPTH macros)
    Track-A/A1-simulator/testcases/sim_public/benchmark/basic05/rtl/dut.v (memory, synchronous q, parameterized leaves, $time, and $dumpvars)
    Track-A/A1-simulator/testcases/sim_public/benchmark/basic05/tb/tb.v (posedge/#1 loop and existing file I/O)
  </read_first>
  <action>
    Per D-07, consume the existing preprocessor output for the two def.v-backed fixtures and preserve relative include resolution; do not add a general SystemVerilog preprocessor. Per D-08 and D-10, extend buildModule/runtime storage and eval_expr.cpp so declared unpacked memories retain address bounds and element width, `$readmemh` fills them from testcase-relative init.hex, `$readmemb` reaches the same loader with binary token parsing, and variable memory indexing reads/writes the selected element without colliding with hierarchical signal names. Implement the parameter overrides and named/positional port bindings used by gen_cell, add_leaf, and basic05_leaf, including generated basic03 child instances, with the existing dot-prefixed flattening convention. Per D-09, return the current scheduler time for `$time`, preserve existing `$fopen`/`$fgets`/`$fscanf`/`$fdisplay`/`$display` formatting, make `$finish` flush and close existing handles, and make `$dumpvars` executable as a no-crash compatibility path without implementing full VCD. Adjust the Makefile recipes only as needed so compile_sim and the no-positional-argument run execute with an absolute testcase FILELIST and testcase working directory, while preserving the Phase 5 interface and not introducing incremental compilation. Run basic04 immediately after its implementation, then basic05, fixing only target-path/runtime issues revealed by their exact diffs.
  </action>
  <acceptance_criteria>
    basic04 loads init.hex and produces correct y values through variable memory selection and parameterized add_leaf instances; basic05 loads its memory, evaluates popcount/selected data and parameterized leaves, updates q on synchronous reset/enabled posedges, reports the current time through existing display formatting, and survives dumpvars; both use the existing file-I/O path and finish cleanly. Includes/macros resolve through the completed frontend, and the Makefile invokes the target cases from their testcase directories without stale output dependence.
  </acceptance_criteria>
  <verification>
    Execute basic04 and basic05 independently in the locked order, each from clean state through build, compile_sim, argument-free run, and exact diff. Re-run basic03 plus the Phase 5 four-case regression after the shared memory/hierarchy changes.
  </verification>
  <verify>
    <automated>set -euo pipefail; ROOT=/home/kali/Documents/github/AI4S; SIM="$ROOT/Track-A/A1-simulator"; for case in basic04 basic05 basic03 basic01 basic02 alu priority_encoder; do CASE_DIR="$SIM/testcases/sim_public/benchmark/$case"; FILELIST="$CASE_DIR/filelist.txt"; (cd "$CASE_DIR" &amp;&amp; make -C "$SIM" clean &amp;&amp; make -C "$SIM" build &amp;&amp; make -C "$SIM" compile_sim FILELIST="$FILELIST" TOP=tb &amp;&amp; make -C "$SIM" run FILELIST="$FILELIST" TOP=tb &amp;&amp; diff -u tb/output_ref.mem tb/output.mem); done</automated>
  </verify>
  <must_haves>
    - "basic04 and basic05 each pass their exact output reference after clean rebuilds."
    - "Memory declarations, parameterized child instances, and target port connections are executable rather than parse-only metadata."
    - "readmemh/readmemb, current time, existing file I/O, finish, and dumpvars do not crash or silently bypass output generation."
    - "i2c/ip/axis_fifo/sha256 and full VCD are not added to the acceptance gate or implemented as hidden scope."
  </must_haves>
  <threat_model>
    <trust_boundaries>
      <boundary>RTL include/macro and filelist contents to runtime metadata</boundary>
      <boundary>Memory indices and system-task file paths to host-backed runtime state</boundary>
      <boundary>Parameterized child ports to flattened parent signals</boundary>
    </trust_boundaries>
    <stride>
      <threat id="T-06-03" category="Tampering" severity="medium" disposition="mitigate">Resolve include and memory paths from the testcase runtime context, validate file handles, and preserve declared memory bounds before writes.</threat>
      <threat id="T-06-04" category="Elevation of Privilege" severity="high" disposition="mitigate">Bind only the target module's declared ports and isolate each child with a deterministic prefix so one instance cannot overwrite another.</threat>
    </stride>
  </threat_model>
  <artifacts>
    - "Memory-aware target elaboration and lvalue execution in Track-A/A1-simulator/src/sim_engine.cpp"
    - "Variable-index/memory expression reads in Track-A/A1-simulator/src/eval_expr.cpp"
    - "Testcase-relative compile_sim/run contract in Track-A/A1-simulator/Makefile"
    - "Exact basic04/basic05 output evidence"
  </artifacts>
  <done>D-07 through D-11 are implemented only for the three target fixture paths, basic04 and basic05 pass exact diff, and all earlier gates remain green.</done>
</task>

<task type="auto">
  <name>Task 3: Run the final clean critical-path acceptance matrix</name>
  <files>Track-A/A1-simulator/Makefile, Track-A/A1-simulator/src/sim_engine.cpp, Track-A/A1-simulator/src/eval_expr.cpp</files>
  <dependencies>Task 2 target semantics and its basic03/basic04/basic05 exact diffs must be complete.</dependencies>
  <read_first>
    .planning/phases/06-a1-advanced-features/06-CONTEXT.md (D-01, D-02, D-03, and deferred scope)
    .planning/phases/05-a1-basic-simulator/05-01-SUMMARY.md (known Phase 5 zero-diff cases)
    Track-A/A1-simulator/Makefile (final build/compile_sim/run contract)
    Track-A/A1-simulator/testcases/sim_public/benchmark/basic03/tb/output_ref.mem (sequential target reference)
    Track-A/A1-simulator/testcases/sim_public/benchmark/basic04/tb/output_ref.mem (memory target reference)
    Track-A/A1-simulator/testcases/sim_public/benchmark/basic05/tb/output_ref.mem (hierarchy/sequential target reference)
    Track-A/A1-simulator/testcases/sim_public/benchmark/basic01/tb/output_ref.mem (Phase 5 regression reference)
    Track-A/A1-simulator/testcases/sim_public/benchmark/basic02/tb/output_ref.mem (Phase 5 regression reference)
    Track-A/A1-simulator/testcases/sim_public/benchmark/alu/tb/output_ref.mem (Phase 5 regression reference)
    Track-A/A1-simulator/testcases/sim_public/benchmark/priority_encoder/tb/output_ref.mem (Phase 5 regression reference)
  </read_first>
  <action>
    Per D-02 and D-03, run the final matrix in this exact order: basic03, basic04, basic05, basic01, basic02, alu, priority_encoder. For each case, start from a clean simulator/parser/output state, build, invoke compile_sim with an absolute testcase filelist and TOP=tb, invoke the Makefile run target with no positional runtime arguments, and compare tb/output.mem to tb/output_ref.mem using diff -u. Treat any build failure, parser failure, missing output, hang, nonzero command, stale output, or nonempty diff as a defect. If a failure appears, fix the smallest underlying shared runtime or Makefile issue in the files owned by this plan and rerun the entire matrix from clean state; do not broaden the scope to deferred cases or performance work. Record the seven zero-diff results in the generated phase summary when execution completes.
  </action>
  <acceptance_criteria>
    All seven cases pass the complete clean build -> compile_sim -> no-argument run -> exact diff sequence in the locked order, with basic03/basic04/basic05 as the immediate Phase 6 gate and basic01/basic02/alu/priority_encoder as the preserved Phase 5 regression. No deferred requirement is represented as a passing claim.
  </acceptance_criteria>
  <verification>
    The automated matrix itself is the acceptance verification; it must be rerun from a clean state after any fix and must exit on the first nonzero command or diff.
  </verification>
  <verify>
    <automated>set -euo pipefail; ROOT=/home/kali/Documents/github/AI4S; SIM="$ROOT/Track-A/A1-simulator"; for case in basic03 basic04 basic05 basic01 basic02 alu priority_encoder; do CASE_DIR="$SIM/testcases/sim_public/benchmark/$case"; FILELIST="$CASE_DIR/filelist.txt"; (cd "$CASE_DIR" &amp;&amp; make -C "$SIM" clean &amp;&amp; make -C "$SIM" build &amp;&amp; make -C "$SIM" compile_sim FILELIST="$FILELIST" TOP=tb &amp;&amp; make -C "$SIM" run FILELIST="$FILELIST" TOP=tb &amp;&amp; diff -u tb/output_ref.mem tb/output.mem); done</automated>
  </verify>
  <must_haves>
    - "The seven-case matrix exits zero with seven empty diffs."
    - "The first three cases are independently auditable as the narrowed Phase 6 acceptance gate."
    - "No claim is made for i2c/ip/axis_fifo/sha256, full VCD, broad async reset, incremental compilation, GEMM, multicore, or performance."
  </must_haves>
  <threat_model>
    <trust_boundaries>
      <boundary>Makefile variables and testcase working directories to simulator invocation</boundary>
      <boundary>Generated output files to exact-diff evidence</boundary>
    </trust_boundaries>
    <stride>
      <threat id="T-06-05" category="Repudiation" severity="medium" disposition="mitigate">Remove generated artifacts before every case and fail fast on every command so stale output cannot certify a passing diff.</threat>
      <threat id="T-06-06" category="Tampering" severity="medium" disposition="mitigate">Use absolute filelist paths, explicit top selection, and direct reference-file comparison for each case.</threat>
    </stride>
  </threat_model>
  <artifacts>
    - "Seven-case clean exact-diff acceptance evidence"
    - "Final phase summary recording target and regression results"
    - "No new implementation scope beyond the files and semantics listed in Tasks 1-2"
  </artifacts>
  <done>D-01 through D-03 are evidenced by the seven-case critical-path matrix, with the three target cases and four Phase 5 regressions all exact zero-diff from clean state.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RTL source/filelist to parser/runtime | Fixture source, includes, macros, and system-task arguments determine executable state. |
| Scheduler/processes to signal state | Event ordering and NBA commit determine observable sequential behavior. |
| Runtime paths/output files to acceptance evidence | CWD, stale artifacts, and output comparison determine whether a gate is trustworthy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-06-SC | Tampering | Build/runtime dependency installation | low | accept | No package install or external dependency is introduced by this plan; existing flex, bison, and g++ toolchain is reused. |
| T-06-01 | Denial of Service | Targeted event queue and delta propagation | high | mitigate | Bound event/delta progress and terminate deterministically on queue exhaustion or finish. |
| T-06-03 | Tampering | Include, memory, and file-I/O paths | medium | mitigate | Use existing relative include provenance, testcase-relative runtime paths, handle checks, and memory bounds. |
| T-06-04 | Elevation of Privilege | Parameterized child port binding | high | mitigate | Validate target fixture connections and isolate prefixed child scopes. |
| T-06-05 | Repudiation | Clean exact-diff matrix | medium | mitigate | Clean before each case, fail fast, use absolute filelists, and compare directly against references. |
</threat_model>

<verification>
Run the final seven-case matrix in order: basic03, basic04, basic05, basic01, basic02, alu, priority_encoder. Every case must execute clean build, compile_sim, Makefile run with no positional runtime arguments, and exact diff -u with zero exit status. The first three cases are the narrowed Phase 6 gate; the last four are mandatory Phase 5 regression evidence.
</verification>

<success_criteria>
1. basic03, basic04, and basic05 each pass clean build, compile_sim, no-argument run, and exact zero-diff output.
2. basic01, basic02, alu, and priority_encoder remain exact zero-diff after the runtime changes.
3. The runtime implements only the locked target semantics: posedge clock, edge wait, #delay, basic NBA plus post-NBA combinational propagation, synchronous reset as needed, existing include/object macro behavior, readmemh/minimal readmemb, memory indexing, target parameterized child instances, $time, existing file I/O, $finish, and non-crashing dumpvars.
4. i2c, ip, axis_fifo, sha256, full VCD, broad async/reset semantics, incremental compilation, GEMM, multicore, and performance are explicitly deferred and are not required for this plan.
</success_criteria>

<output>
Create `.planning/phases/06-a1-advanced-features/06-02-SUMMARY.md` when done.
</output>
