---
phase: 06-a1-advanced-features
plan: 04
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - Track-A/A1-simulator/Makefile
  - Track-A/A1-simulator/testcases/sim_public/test_interface/test_makefile_interface.py
autonomous: true
requirements:
  - A1-CASE-01
must_haves:
  truths:
    - "Every required Phase 6 testcase passes clean build, compile_sim, run, and exact diff against output_ref.mem."
    - "The Phase 5 basic01, basic02, alu, and priority_encoder cases still pass with exact zero diff after the sequential and hierarchy changes."
    - "The Makefile keeps build, compile_sim, run, parallel_run, and clean behavior deterministic from any testcase working directory."
  artifacts:
    - path: "Track-A/A1-simulator/Makefile"
      provides: "Clean-build and deterministic compile/run interface"
    - path: "Track-A/A1-simulator/testcases/sim_public/test_interface/test_makefile_interface.py"
      provides: "Interface regression coverage for required targets"
  key_links:
    - from: "Makefile compile_sim/run targets"
      to: "runtime working directory"
      via: "absolute simulator paths with testcase-relative FILELIST and output.mem"
    - from: "Makefile clean target"
      to: "clean regression loop"
      via: "generated parser, simulator, serialized output, and testcase output are removed before each case"
    - from: "clean regression loop"
      to: "all output_ref.mem files"
      via: "each case runs compile_sim, run, then exact diff"
---

<objective>
Close Phase 6 with deterministic Makefile behavior and a clean exact-diff regression over all advanced targets plus the complete Phase 5 baseline.

Purpose: D-11 and D-12 make correctness evidence the phase gate. The simulator is not complete because it merely runs or partially matches output; every target must pass from clean artifacts, and Phase 5 behavior must remain intact.

Output: Interface regression coverage and auditable zero-diff results for basic03/basic04/basic05/i2c/ip/axis_fifo/sha256 plus basic01/basic02/alu/priority_encoder.
</objective>

## Multi-source coverage audit

| Source | Item | Plan | Status | Notes |
|---|---|---|---|---|
| GOAL | Sequential RTL, multi-file hierarchy, and exact zero-diff completion for basic03/basic04/basic05/i2c/ip/axis_fifo/sha256 | 06-01 through 06-04 | COVERED | Frontend, scheduler, hierarchy, and final matrix tasks cover the full phase outcome. |
| REQ | A1-PARSE-07 | 06-01 | COVERED | Recursive macro/include preprocessing is implemented and verified. |
| REQ | A1-SIM-02, A1-SIM-03, A1-SIM-04 | 06-02 | COVERED | Active/NBA/delta scheduling, reset edges, memory/time/I/O/system functions, and dump state are planned. |
| REQ | A1-CASE-01 | 06-04 | COVERED | Phase 5 basic01/basic02/alu/priority_encoder regression is mandatory. |
| REQ | A1-CASE-04, A1-CASE-05, A1-CASE-06, A1-CASE-07 | 06-03 and 06-04 | COVERED | i2c/ip/axis_fifo/sha256 smoke and final exact-diff gates are mandatory. |
| RESEARCH | No `06-RESEARCH.md` exists | N/A | COVERED | No separate research constraints or package decisions were supplied; planning uses the locked context and established Phase 5 implementation. |
| CONTEXT | D-01 through D-04 | 06-02 | COVERED | Discrete queue, Active/NBA/delta, reset edges, exhaustion, and finish cleanup are cited in scheduler actions. |
| CONTEXT | D-05 through D-08 | 06-01 and 06-02 | COVERED | Macro/include, path bases, readmem ranges, time, display, and observable dump state are cited. |
| CONTEXT | D-09 through D-10 | 06-01 through 06-03 | COVERED | Global collection, recursive elaboration, packed/unpacked memory, and variable indexing are cited. |
| CONTEXT | D-11 through D-12 | 06-03 and 06-04 | COVERED | Priority order, clean build, compile, run, and exact diff gates are cited. |
| CONTEXT | Phase 7 deferral | N/A | EXCLUDED | Incremental compilation remains explicitly deferred to Phase 7 and is not implemented by this phase. |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-a1-basic-simulator/05-01-SUMMARY.md
@.planning/phases/06-a1-advanced-features/06-CONTEXT.md
@.planning/phases/06-a1-advanced-features/06-01-PLAN.md
@.planning/phases/06-a1-advanced-features/06-02-PLAN.md
@.planning/phases/06-a1-advanced-features/06-03-PLAN.md
@Track-A/A1-simulator/Makefile
@Track-A/A1-simulator/src/preprocessor.h
@Track-A/A1-simulator/src/preprocessor.cpp
@Track-A/A1-simulator/src/sim_engine.cpp
@Track-A/A1-simulator/testcases/sim_public/test_interface/Makefile
@Track-A/A1-simulator/testcases/sim_public/test_interface/test_makefile_interface.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Harden deterministic Makefile build and runtime interface</name>
  <files>Track-A/A1-simulator/Makefile</files>
  <dependencies>06-03 hierarchy and runtime integration</dependencies>
  <read_first>
    Track-A/A1-simulator/Makefile (current absolute simulator and testcase CWD handling)
    Track-A/A1-simulator/testcases/sim_public/test_interface/Makefile (reference interface checks and isolated CWD expectations)
    .planning/phases/05-a1-basic-simulator/05-01-SUMMARY.md (Phase 5 clean regression result)
    .planning/phases/06-a1-advanced-features/06-CONTEXT.md (D-06, D-11, and D-12)
  </read_first>
  <action>
Make the A1 Makefile interface deterministic for clean-build verification: `build` regenerates flex/bison artifacts, `compile_sim` consumes a testcase-relative FILELIST and TOP, `run` executes with no extra arguments from the testcase directory, `parallel_run` remains a compatible entry point, and `clean` removes simulator, serialized output, generated parser files, and testcase output. Keep simulator and generated-artifact paths absolute where `make -C` changes directory, preserve runtime-relative `$fopen`/memory-loader behavior from D-06, and ensure the simulator does not depend on the caller's source-tree directory. Do not add incremental compilation here; that remains Phase 7 scope per the locked context.
  </action>
  <verify>
    <automated>cd /home/kali/Documents/github/AI4S/Track-A/A1-simulator &amp;&amp; make clean &amp;&amp; make build &amp;&amp; test -x simulator</automated>
    <automated>cd /home/kali/Documents/github/AI4S/Track-A/A1-simulator/testcases/sim_public/test_interface &amp;&amp; python3 test_makefile_interface.py</automated>
  </verify>
  <acceptance_criteria>Build, compile_sim, run, parallel_run, and clean work from testcase directories with deterministic paths; clean removes generated build/runtime artifacts; no Phase 7 incremental-compilation behavior is introduced.</acceptance_criteria>
  <done>The Makefile contract is stable for every Phase 6 and Phase 5 regression invocation.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic Makefile interface regression coverage</name>
  <files>Track-A/A1-simulator/Makefile, Track-A/A1-simulator/testcases/sim_public/test_interface/test_makefile_interface.py</files>
  <dependencies>Task 1 deterministic interface</dependencies>
  <read_first>
    Track-A/A1-simulator/Makefile (current absolute simulator and testcase CWD handling)
    Track-A/A1-simulator/testcases/sim_public/test_interface/Makefile (reference interface checks and isolated CWD expectations)
    Track-A/A1-simulator/testcases/sim_public/test_interface/test_makefile_interface.py (existing test harness conventions)
    .planning/phases/05-a1-basic-simulator/05-01-SUMMARY.md (Phase 5 clean regression result)
    .planning/phases/06-a1-advanced-features/06-CONTEXT.md (D-11 and D-12)
  </read_first>
  <action>
Make the A1 Makefile interface explicit for clean-build verification: `build` regenerates flex/bison artifacts, `compile_sim` consumes a testcase-relative FILELIST and TOP, `run` executes with no extra arguments from the testcase directory, `parallel_run` remains a compatible entry point, and `clean` removes simulator, serialized output, generated parser files, and testcase output. Extend the interface test harness with a table-driven command contract for the seven Phase 6 cases and the four Phase 5 cases, checking required files, exit status, and exact diff command shape without hardcoding output counts. Keep all paths absolute where the Makefile changes CWD and do not make the simulator depend on the caller's source-tree directory. Do not add incremental compilation here; that remains Phase 7 scope per the locked context.
  </action>
  <verify>
    <automated>cd /home/kali/Documents/github/AI4S/Track-A/A1-simulator/testcases/sim_public/test_interface &amp;&amp; python3 test_makefile_interface.py</automated>
    <automated>cd /home/kali/Documents/github/AI4S/Track-A/A1-simulator &amp;&amp; make clean &amp;&amp; make build &amp;&amp; test -x simulator</automated>
  </verify>
  <acceptance_criteria>Interface tests cover all required targets and verify build/compile_sim/run/parallel_run/clean path behavior; the Makefile can be invoked from each testcase directory without path-dependent failures.</acceptance_criteria>
  <done>Makefile contract and regression harness are deterministic for the entire A1 target set.</done>
</task>

<task type="auto">
  <name>Task 3: Run final clean exact-diff regression for Phase 6 and Phase 5</name>
  <files>Track-A/A1-simulator/Makefile, Track-A/A1-simulator/testcases/sim_public/test_interface/test_makefile_interface.py</files>
  <dependencies>Tasks 1-2; all functional implementation plans complete</dependencies>
  <read_first>
    .planning/phases/06-a1-advanced-features/06-CONTEXT.md (D-11 priority and D-12 completion rule)
    .planning/phases/05-a1-basic-simulator/05-01-SUMMARY.md (known Phase 5 passing cases and regression command)
    Track-A/A1-simulator/testcases/sim_public/benchmark/basic03/tb/output_ref.mem
    Track-A/A1-simulator/testcases/sim_public/benchmark/basic04/tb/output_ref.mem
    Track-A/A1-simulator/testcases/sim_public/benchmark/basic05/tb/output_ref.mem
    Track-A/A1-simulator/testcases/sim_public/benchmark/i2c/tb/output_ref.mem
    Track-A/A1-simulator/testcases/sim_public/benchmark/ip/tb/output_ref.mem
    Track-A/A1-simulator/testcases/sim_public/benchmark/axis_fifo/tb/output_ref.mem
    Track-A/A1-simulator/testcases/sim_public/benchmark/sha256/tb/output_ref.mem
    Track-A/A1-simulator/testcases/sim_public/benchmark/basic01/tb/output_ref.mem
    Track-A/A1-simulator/testcases/sim_public/benchmark/basic02/tb/output_ref.mem
    Track-A/A1-simulator/testcases/sim_public/benchmark/alu/tb/output_ref.mem
    Track-A/A1-simulator/testcases/sim_public/benchmark/priority_encoder/tb/output_ref.mem
  </read_first>
  <action>
Execute the final matrix in D-11 order: basic03, basic04, basic05 first; then i2c, ip, axis_fifo, sha256; then the Phase 5 regression basic01, basic02, alu, priority_encoder. For every case, remove generated simulator/parser/output artifacts, run `make clean`, run `make build`, invoke `compile_sim FILELIST=filelist.txt TOP=tb`, invoke argument-free `make run`, and compare `tb/output.mem` to `tb/output_ref.mem` with exact `diff -u`. Record each case as pass only when every command succeeds and the diff is empty. If any case fails, fix the underlying runtime/elaboration/Makefile issue and rerun the entire matrix from clean state; partial matches, stale outputs, nonzero stdout differences, and non-crashing execution are failures under D-12.
  </action>
  <verify>
    <automated>set -euo pipefail; ROOT=/home/kali/Documents/github/AI4S; SIM="$ROOT/Track-A/A1-simulator"; for case in basic03 basic04 basic05 i2c ip axis_fifo sha256 basic01 basic02 alu priority_encoder; do CASE_DIR="$SIM/testcases/sim_public/benchmark/$case"; (cd "$CASE_DIR" &amp;&amp; make -C "$SIM" clean &amp;&amp; make -C "$SIM" build &amp;&amp; make -C "$SIM" compile_sim FILELIST=filelist.txt TOP=tb &amp;&amp; make -C "$SIM" run &amp;&amp; diff -u tb/output_ref.mem tb/output.mem); done</automated>
    <automated>cd /home/kali/Documents/github/AI4S/Track-A/A1-simulator &amp;&amp; test -x simulator</automated>
  </verify>
  <acceptance_criteria>All ten cases pass from clean state, each has an empty exact diff, and the Phase 5 four-case regression remains green after Phase 6 changes.</acceptance_criteria>
  <done>Phase 6 satisfies D-11/D-12 and produces auditable zero-diff evidence for every requested target.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Makefile variables and testcase CWD to simulator | FILELIST, TOP, output, and runtime paths cross the command boundary. |
| Regression outputs to acceptance evidence | Stale files or incomplete commands could falsely certify a case. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-06-11 | Spoofing | Makefile FILELIST/TOP inputs | medium | mitigate | Validate readable filelists, explicit top selection, absolute simulator paths, and testcase-local runtime outputs. |
| T-06-12 | Repudiation | exact-diff regression record | medium | mitigate | Run each case from clean state with command failure propagation and direct `diff -u`; do not treat stale output as evidence. |
</threat_model>

<verification>
The final command matrix must execute clean build, compile_sim, run, and exact diff in D-11 order for basic03/basic04/basic05, i2c, ip, axis_fifo, sha256, and the Phase 5 basic01/basic02/alu/priority_encoder regression. Interface tests and executable checks must also pass.
</verification>

<success_criteria>
1. Makefile targets work from testcase directories with deterministic paths.
2. All ten required cases pass exact zero-diff clean-build verification.
3. Phase 5 behavior is preserved and its four-case regression remains green.
4. Incremental compilation remains outside Phase 6 and is traceable to Phase 7.
</success_criteria>

## Artifacts this phase produces

- Deterministic A1 Makefile interface and regression coverage
- Clean exact-diff evidence for seven Phase 6 cases plus four Phase 5 cases
- Final simulator binary and generated compile artifacts produced by the verified build path

<output>
Create `.planning/phases/06-a1-advanced-features/06-04-SUMMARY.md` when done.
</output>
